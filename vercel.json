{
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": { "distDir": "dist" }
		}
	],
	"scripts": {
		"dev": "bun run astro dev",
		"build": "bun run astro build"
	}
}
